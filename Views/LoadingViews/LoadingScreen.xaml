<Page
    x:Class="QuanLyDaiLy.Views.LoadingViews.LoadingScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="LoadingScreen"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <!--  Right here  -->
    <StackPanel
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Visibility="Visible">
        <Border
            Padding="20"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="White"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="8">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,15"
                    HorizontalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="#333333"
                    Text="Đang lấy dữ liệu" />
                <ProgressBar
                    Width="250"
                    Height="20"
                    Background="#F0F0F0"
                    BorderBrush="Transparent"
                    IsIndeterminate="True" />
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
